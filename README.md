#Frontend template

## 🚀 Технологии

- **React 18** - Библиотека для создания пользовательских интерфейсов
- **Next.js 15** - Фреймворк для React с App Router
- **JavaScript (ES6+)** - Современный JavaScript
- **Tailwind CSS** - Utility-first CSS фреймворк
- **Yarn** - Менеджер пакетов
- **ESLint** - Статический анализ кода
- **Prettier** - Форматирование кода

## 📁 Структура проекта

```
src/
├── app/          # Next.js App Router страницы
├── components/   # Переиспользуемые компоненты
├── utils/        # Утилиты и вспомогательные функции
├── hooks/        # Кастомные React хуки
└── pages/        # Дополнительные страницы
```

## 🔧 Кастомные алиасы импортов

В проекте настроены удобные алиасы для импортов **без использования @**:

```javascript
// Компоненты
import Header from 'components/Header';
import Button from 'components/Button';

// Хуки
import { useAuth } from 'hooks/useAuth';
```

Алиасы настроены в:

- `jsconfig.json` - для JavaScript
- `next.config.mjs` - для Webpack

## 📦 Управление пакетами

Проект использует **Yarn** как единственный пакетный менеджер. Использование npm запрещено через:

### Защита от npm:

1. **`.npmrc`** - файл с настройками engine-strict
2. **`package.json`** - engines с "npm": "please-use-yarn"
3. **`preinstall` скрипт** - блокирует npm install
4. **`packageManager`** - явно указывает yarn@1.22.22

### Сообщения об ошибках:

```bash
# При попытке использовать npm install
npm error engine Unsupported engine
npm error engine Not compatible with your version of node/npm
npm error notsup Required: {"npm":"please-use-yarn"}

# При попытке использовать npm в некоторых случаях
❌ Используйте yarn для установки зависимостей. npm запрещен в этом проекте.
```

## 🛠 Установка и запуск

### ⚠️ Важно: Используйте только Yarn

Проект настроен для использования **только Yarn**. Использование npm **запрещено** и заблокировано.

```bash
# ✅ Правильно - используйте yarn
yarn install

# ❌ Неправильно - npm заблокирован
npm install  # Выдаст ошибку
```

### Установка зависимостей

```bash
yarn install
```

### Запуск в режиме разработки

```bash
yarn dev
```

Проект будет доступен по адресу: http://localhost:3000

### Сборка для продакшена

```bash
yarn build
```

### Запуск продакшен сборки

```bash
yarn start
```

## 📝 Полезные команды

```bash
# Линтинг (Next.js)
yarn lint

# Исправление ошибок линтера (Next.js)
yarn lint:fix

# Кастомный линтинг (только src/)
yarn lint:custom

# Исправление ошибок кастомного линтера (только src/)
yarn lint:custom:fix

# Линтинг всех файлов (src/ + scripts/ + config/)
yarn lint:all

# Исправление ошибок во всех файлах
yarn lint:all:fix

# Форматирование кода
yarn format

# Проверка форматирования
yarn format:check

# Проверка кода и форматирования (все файлы)
yarn run check

# Исправление и форматирование (все файлы)
yarn run fix
```

## 🎯 Особенности

- ✅ Современная архитектура с App Router
- ✅ Чистый JavaScript (ES6+)
- ✅ Кастомные алиасы импортов
- ✅ ESLint для контроля качества кода
- ✅ Prettier для единообразного форматирования
- ✅ Автоматическая сортировка импортов
- ✅ Принудительное использование Yarn (npm заблокирован)
- ✅ Оптимизированная сборка
- ✅ Готовая структура для масштабирования

## 🔄 Разработка

При разработке используйте кастомные алиасы для импортов:

```javascript
// ✅ Правильно
import MyComponent from 'components/MyComponent';

// ❌ Неправильно
import MyComponent from '../components/MyComponent';
```

### Правила использования console.log:

```javascript
// ❌ Запрещено в src/ файлах
// src/components/MyComponent.jsx
console.log('Debug info'); // ESLint ошибка!

// ✅ Разрешено в scripts/ файлах
// scripts/build.js
console.log('Build completed'); // OK

// ✅ Разрешено в конфигурационных файлах
// next.config.mjs
console.log('Config loaded'); // OK
```

Для отладки в `src/` используйте:

- Debugger браузера
- React DevTools
- Временные комментарии
- Тесты

## 🎨 Стиль кода

Проект использует современную конфигурацию ESLint + Prettier:

- **ESLint** - для поиска проблем в коде
- **Prettier** - для автоматического форматирования
- **EditorConfig** - для единообразия настроек редактора

### Настройки форматирования:

- Использование одинарных кавычек
- Точки с запятой включены
- Trailing commas в ES5 стиле
- Ширина строки: 80 символов
- Отступы: 2 пробела
- Пустая строка в конце каждого файла
